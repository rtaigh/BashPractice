#!/bin/bash


echo "shebang is an abbreviation of \"hash bang\". 
It is the name given to the first line of a shell script.
The most common modern day shell scripts are written in bash (born again shell).
bash's shebang is #!/bin/bash. " 
# shebang is the path to the (program executable) bin bash directory if creating a bash script.
# there are other sheels like korn and zsh but bash is the most common.
